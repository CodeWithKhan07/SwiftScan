# FatoraLens release rules.
# Flutter plugins used by this project publish their own consumer keep rules.
# Add app-specific rules here only when release shrinking is enabled and a
# concrete release-only issue requires them.
